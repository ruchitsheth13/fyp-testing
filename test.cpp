#include <iostream>

int main(int argc, char** argv) {
  int t = 5; // Initialize variable 't' with a valid value
  int x = 10; // Declare variable 'x' before using it
  std::cout << "Hello World!" << std::endl;
  return x;
}